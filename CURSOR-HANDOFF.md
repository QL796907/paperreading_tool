# Cursor 落地说明（直接丢这个 + UI-design-complete.md）

## 产品

本地网页「术语本 / Paper Glossary」：Zotero 选词入库 + 手写添加 + 本机 OpenAI 兼容 API 解析 + 按**首次填写日**落页 + 搜索。已有可运行实现时，**改 UI / 补展页阅读**，不要另起产品。

## 必读

1. `UI-design-complete.md` — 完整设计（token、各屏、展页分页与翻页、验收清单）
2. `mocks/` — 示意 PNG（风格参考；实现比图更克制，少钢笔/缎带/装饰花）

## 本次相对旧稿的增量

- **展页阅读模式**：选中日期后进入；单页纸放大；像读书 App **一页一页翻**；日内分页；翻过末页进入下一有词日；`Esc`/返回退出。
- 补全：设置、搜索结果、Toast、删除确认、Zotero 四态、窄屏、减动效、验收清单与实现顺序。

## 展页阅读（实现要点）

- 入口：右页「展页阅读」或快捷键 `F`
- 布局：藏目录；居中单页纸 max-width ~720px
- 分页：按**完整词条块**切页；页码为日内 `当前/总页`
- 翻页：左右热区 + `←/→` + Space/PageDown + 可选横滑
- 跨日：跳过无词日；退出后目录选中 = 当前阅读日
- 动效：水平滑 ~280ms；尊重 `prefers-reduced-motion`
- 视觉：同一套 cream/朱红/森林绿/宋体，**不要**黑底 Immersive Reader

## 不要做

紫渐变、底 Tab、大阴影卡片堆、Notion 数据库风、背单词进度、整页 spinner、强制 3D 翻书、展页左右双页杂志。

## 建议 PR 切片

1. Token + 主骨架  
2. 目录/词卡/空态/搜索/overlay/Toast  
3. **展页阅读 + 分页翻页跨日**  
4. Zotero 按钮态 + 窄屏  
