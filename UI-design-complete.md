# Paper Glossary · 术语本 — 完整 UI 设计稿（落地用）

> 给实现用。桌面网页优先 · 约 1280×800 · 简体中文 · 对齐已有本地术语本能力，不另起产品。  
> 英文产品名 `Paper Glossary` 仅作眉题。

---

## 0. 给 Cursor 的一句话范围

实现一套「纸质学术笔记本」风格的本地术语本 UI：左日期目录 + 右当日词条；支持搜索、添加、设置、Zotero 加入、Toast；**选中某日后可进入展页阅读模式，像读书 App 一样一页一页翻**（纸感与主界面一致）。不要做成背单词 App / Notion / SaaS 仪表盘。

---

## 1. 设计理念（≤120 字）

术语本不是效率仪表盘，而是摊开在暗木桌上的研究笔记。左页日期目录，右页当日稿纸；朱红栏线托住墨色词头，森林绿只点在状态与动作上。展页阅读时收起目录，单页纸放大铺满阅读区，左右翻页如翻书。一切为「选词 → 落页 → 可搜可改 → 可静读」服务。

---

## 2. 设计系统

### 2.1 色彩 Token

| Token | Hex | 用途 |
|--------|------|------|
| `--desk` | `#2C241C` | 满屏桌面衬底 |
| `--desk-edge` | `#1A1510` | 书脊/阴影暗部 |
| `--paper` | `#F4ECD8` | 主纸张 |
| `--paper-aged` | `#EBE2CC` | 目录页、选中行、斑马感 |
| `--paper-shadow` | `rgba(44,36,28,0.18)` | 书页落影（极轻） |
| `--ink` | `#1F1A14` | 主墨 |
| `--ink-muted` | `#6B5E4E` | 次级文案 |
| `--ink-faint` | `#A89880` | 占位/禁用 |
| `--rule` | `#C45C48` | 朱红栏线、强调 |
| `--rule-soft` | `rgba(196,92,72,0.35)` | 横格线 |
| `--accent` | `#2F4F3E` | 主按钮、选中日、成功 |
| `--accent-soft` | `#E4EDE7` | 浅绿底 |
| `--seal-pending` | `#8B6914` | 待解析章 |
| `--seal-ai` | `#2F4F3E` | AI 解析章 |
| `--seal-hand` | `#5C4033` | 手写章 |
| `--danger` | `#8B3A2F` | 删除/失败 |
| `--toast-bg` | `#1F1A14` | Toast 底 |
| `--toast-fg` | `#F4ECD8` | Toast 字 |
| `--scrim` | `rgba(26,21,16,0.45)` | Overlay 幕布 |

**禁止：** 紫渐变、霓虹高饱和、大阴影卡片堆、玻璃拟态、无衬线仪表盘、底部 Tab。

### 2.2 字体

| 角色 | 建议 | 回退 |
|------|------|------|
| 中文正文 | Noto Serif SC / 思源宋体 | Songti SC, serif |
| 中文词头 | 同上 700 | — |
| 西文眉题 | EB Garamond / Cormorant Garamond | Georgia |
| 等宽 | IBM Plex Mono | ui-monospace |

正文行高 **1.75**；词头 **1.25**。展页阅读正文可 **17–18px**，行高 **1.85**。

### 2.3 间距与网格

- 基准 **8px**；页边距 **24–32px**。
- 右页横格周期 **28px**（含行）；横格色 `--rule-soft`。
- 目录行高 **36px**；月标题距首日 **16px**。
- 词条之间：细朱红分隔或 **14px** 空白，**不用** 大卡片阴影。
- 圆角尽量少：纸/按钮/章 **≤2px**，可直角。

### 2.4 两种主骨架

**A. 展开放（默认工作态）**

```
desk 满屏
└─ 书本 ~1120×720 居中
   ├─ 左目录 ~280
   ├─ 装订暗缝
   └─ 右当日页（搜索 / 词条 / 空状态）
```

**B. 展页阅读（沉浸读书态）**

```
desk 满屏（可略更暗）
└─ 单页纸 ~720×860 居中（或宽约视口 56–62%）
   ├─ 顶：日期 + 页码 + 退出
   ├─ 中：本页词条流（稿纸横格）
   └─ 左右热区 / 箭头：上一页 · 下一页
目录隐藏；可底部细条显示「9月17日 · 2/5」
```

窄屏 ≤900：默认仅右页；「目录」抽屉；展页阅读纸宽接近满宽减 24px。

---

## 3. 信息架构与导航

```
主界面（展开书）
├─ 左：按月日期目录（选日）
├─ 右：当日词条 | 空状态 | 搜索结果流
├─ 添加术语 overlay
├─ 设置 overlay
├─ 删除确认纸片
└─ 展页阅读模式 ← 从「选中日」进入
     └─ 按「纸页」翻（见 §5），可跨日
```

Zotero 为外部选区弹窗上的「加入术语本」按钮（非本页内路由）。

---

## 4. 逐屏说明（工作态）

### 4.1 笔记本主界面

**左·日期目录**

- 眉题：`术语本` + `Paper Glossary`（西文 muted、Garamond ~13px）
- 月分组：`2026年9月`
- 日期行：`9月17日 · 周三` + 右侧 `3 词`
- 选中：左 **4×20** 森林绿竖条 + `--paper-aged` 底
- 无词日可点 → 右页空状态
- 底可选累计：`共 N 天 · M 词`（muted，非必须）

**右·当日页**

- 大日期：`九月十七日`
- 工具：搜索槽、`添加术语`、`展页阅读`（或图标「▣/书页」+ 文案）、`设置`
- 主体：词条流贴横格

**键盘（全局工作态）**

| 键 | 行为 |
|----|------|
| `/` 或 `Ctrl+K` | 聚焦搜索 |
| `Ctrl+N` | 打开添加 |
| `Enter`（焦点在目录日）或点「展页阅读」 | 进入展页阅读 |
| `Esc` | 关 overlay；搜索有内容则清空 |

### 4.2 空状态

右页居中偏上，纯文字，无卡通：

```
今天的纸页还是空白。

在 Zotero 里选中生词，点「加入术语本」；
或按 Ctrl+N，手写第一条。
```

首行 `--ink`，其余 muted；下短朱红线宽 ~48px。空日仍可点「展页阅读」——进入后显示同一空态文案于单页纸上，左右可翻到有内容的邻近日。

### 4.3 搜索结果态

- **不** 单独结果页：输入即替换右页内容；左目录保留，选中态弱化。
- 标题改为：`搜索「…」· N 条`
- 每条旁：`写于 9月12日`（可点 → 跳该日并高亮该词；若在展页中则打开该日对应纸页）
- 无结果：`没有找到相关术语。换个写法，或检查论文名拼写。`
- `Esc` / 槽内 × → 回当前选中日
- 搜索打开时：**隐藏或禁用「展页阅读」**（先退出搜索再展页），避免两态冲突

### 4.4 词条卡片

1. 词头 18–20px 粗 + 右侧状态章  
2. 解析正文  
3. 引用块：左 3px `--rule` + 缩进上下文  
4. 来源行（多来源合并，分号分隔）  
5. 操作：`改解析` · `AI 解析` · `删除`（文字链，`·` 分隔）

**状态章：** 待解析 / 手写 / AI 解析 / 解析中（描边方印，高 20–22，圆角 ≤2，非胶囊）

**业务规则（实现必须）：**

- 同一术语再加入 → **不新增行**；合并来源；留在**首次填写日**那一页；Toast「重复词，已合并来源」
- AI 解析中：仅该条印章区「解析中…」，整页不转圈
- 删除：先确认纸片

### 4.5 添加术语 Overlay

- 幕布 `--scrim`；纸片宽 ~440，`--paper`，圆角 ≤2
- 标题：`添加术语`
- 字段：术语*、解析、上下文、来源论文
- 勾选：`保存后立刻 AI 解析`（方框，非 iOS 开关）
- `取消` / `保存`；术语空 → 红线 + `写一个术语`

### 4.6 设置 Overlay

- 纸片宽 ~480
- 顶说明：`连接 OpenAI 兼容接口。密钥只保存在本机，不会上传。`
- 字段：API 地址、API Key（等宽掩码+显示）、模型名、系统提示词（多行）
- 勾选：`Zotero 新词自动解析`
- `取消` / `保存`

### 4.7 删除确认纸片

- 宽 ~360；文案：`删除「{术语}」？此操作不可撤销。`
- `取消` / `删除`（`--danger` 字或描边）

### 4.8 Toast

- 底居中，书缘上方约 48px；墨底纸字；圆角 ≤2；~3.2s
- 文案：`已加入术语本` / `重复词，已合并来源` / `连不上本地服务，确认术语本已打开`（失败可带「重试」）

---

## 5. 展页阅读模式（重点新增）

### 5.1 定位

类似手机读书 App 的「单页沉浸 + 左右翻页」，但是：

- 视觉仍是 **奶油稿纸 + 朱红栏线 + 宋体词头**，不是纯黑夜间阅读器，也不是平板漫画翻页炫光；
- 从**已选中的某个日期**进入；
- 翻的是「纸页（page）」，不是无限滚动列表。

### 5.2 进入 / 退出

**进入（任一）：**

1. 右页工具栏点 **「展页阅读」**（主入口文案；可用书页图标）  
2. 双击右页空白稿纸区（可选增强）  
3. 键盘：`F` 或 `Enter`（当焦点在目录选中日且无 overlay）  

进入动效（建议 220–280ms）：左目录与装订缝向左收起淡出，右页纸放大居中；desk 略加深。不要弹跳、不要 3D 立体翻书必须项（**优先水平滑动翻页**；若做拟真翻页须极克制、可关）。

**退出（任一）：**

- 顶栏 **「返回」** / `Esc`
- 点击纸页外的 desk 区域  

退出后：回到展开书；**恢复进入前所在日期**；若曾翻到其他日，目录选中同步为当前阅读日。

### 5.3 一屏一纸：分页规则

将「某个日期的词条列表」按阅读纸的可用高度切成多页：

1. 纸内容区高度固定（扣除顶栏与底页码）。  
2. 按词条**完整块**分页：一条词卡不可拆到两页（若单条超一页，则该条内解析区在页内滚动，或整条独占多页并在词头下标 `续`——优先「单条过长则页内小滚」，避免拆词头）。  
3. 页码按 **当前日期内** 计：`2 / 5`（第 2 页 / 共 5 页）。  
4. **跨日：** 在当日最后一页再「下一页」→ 进入**下一个有记录的日期**第 1 页（跳过完全无词的日子，或保留空页仅一页空态——推荐 **跳过无词日**，更像读书）。上一页对称。  
5. 目录里的「词数」不影响分页算法；分页只看实际渲染高度。

空日进入：仅 1 页空态；下一页跳到下一有词日。

### 5.4 展页布局标注

```
┌──────────────── desk ────────────────┐
│  [返回]     九月十七日      2 / 5    │  ← 顶栏浮在纸外或纸顶内边，字 muted
│     ┌──────── 单页纸 ────────┐       │
│     │  词头 + 章               │       │
│     │  解析…                   │       │
│     │  引用 / 来源             │       │
│     │  ──朱红分隔──            │       │
│     │  下一条…                 │       │
│     └─────────────────────────┘       │
│  ‹ 热区          纸外 desk        热区 › │
└──────────────────────────────────────┘
```

- 纸尺寸：高约视口 **78–88%**，宽约 **56–62%**（桌面）；最大宽 ~720px，使行长适合宋体阅读（每行约 28–34 中文字）。  
- 顶栏：左 `返回`，中 `九月十七日`，右 `2 / 5`。跨日翻页时日期与页码同时变。  
- 左右各 **72–96px** 透明点击热区；也可纸左右缘内侧淡箭头（默认隐，hover desk 时显）。  
- 底可选细文案：`共 3 词 · 手写 1 · AI 2`（勿做成工具栏）。  
- **展页内默认隐藏** 改解析 / AI / 删除，避免误触；右上 `⋯` 或长按词头展开操作（桌面：词头旁 `⋯` hover 出菜单）。也可顶栏提供「编辑本页」→ 退出到工作态并定位该日。

### 5.5 翻页交互

| 输入 | 行为 |
|------|------|
| 点击右热区 / 右箭头 | 下一页 |
| 点击左热区 / 左箭头 | 上一页 |
| 键盘 `→` `PageDown` `Space` | 下一页 |
| 键盘 `←` `PageUp` | 上一页 |
| 触板双指横滑 | 跟手翻页（阈值松弛） |
| 点击页码 | 可选：弹出当日迷你页列表（1…N），勿做成进度条游戏感 |

**动效：** 水平位移 + 极轻阴影；时长 ~280ms；ease-out。到尽头（全书第一页的上一页 / 最后有词日最后一页的下一页）轻微回弹，Toast 勿刷；可短震感无。

**边界文案（可选一次）：** `已经是最早一页` / `已经是最新一页`。

### 5.6 展页与其它态冲突

| 场景 | 规则 |
|------|------|
| 打开添加/设置 | 先退出展页，再开 overlay；或展页上叠纸片（推荐 **退出再开**，状态更简单） |
| 搜索 | 展页中 `Ctrl+K` → 退出展页并聚焦搜索 |
| AI 解析中进入展页 | 允许；对应词显示「解析中」章 |
| 从搜索点「写于某日」 | 进工作态该日；若按住 Alt/选项「展页打开」可直接展页（非必须） |

### 5.7 主界面入口文案

- 按钮：`展页阅读`  
- Tooltip：`放大本页，像读书一样翻页（F）`  
- 空状态也可在短文下加文字链：`展页阅读`

---

## 6. Zotero 选区弹窗按钮

嵌入 Zotero 自带高亮弹窗，不抢戏。

| 态 | 文案 | 样式 |
|----|------|------|
| 默认 | 加入术语本 | 与邻钮同高 ~28–32px；无强底；墨色字；可选左细分隔线 |
| 进行中 | 加入中… | 降对比；禁用；静态小点，无炫转 |
| 成功 | 已加入 | `--accent` 字色；~1.2s 后恢复 |
| 失败 | 加入失败 | `--danger`；可再点 |

无阴影、无大块绿底。

---

## 7. 关键组件速查

| 组件 | 规则 |
|------|------|
| 书本容器 | max ~1120×720 |
| 日期行 | h36；选中绿条 4×20 |
| 词头 | 18–20 / 700；短朱红下划可选 |
| 章 | h20–22；pad 6；描边 1；r≤2 |
| 引用块 | 左竖 3px rule；垫 12 |
| 主按钮 | h36；底 accent；字 paper；r≤2 |
| 幽灵按钮 | 同高；描边或文字链 |
| 搜索槽 | h36；底边 rule-soft；聚焦改 rule |
| Overlay 纸片 | pad 28；字段距 16 |
| 展页纸 | max-w ~720；高 ~78–88vh；横格 28 |
| 翻页热区 | 宽 72–96 |

---

## 8. 窄屏（≤900）

- 工作态：只显示右页；左上 `目录` 拉开抽屉（纸色抽屉，非 Material）。  
- 展页：纸宽 `100% - 32px`；热区变窄但保留；顶栏三件套不换行可缩小字号。  
- 不出现底部 Tab。

---

## 9. 动效与无障碍（简）

- 尊重 `prefers-reduced-motion`：展页进入改为淡入；翻页改为瞬切或短淡入。  
- 焦点可见：栏线/底边变 `--rule`，勿只靠色块。  
- 删除与失败 Toast 不要仅靠颜色。

---

## 10. 明确不要

大阴影卡片堆、紫渐变、无衬线仪表盘、底部 Tab、背单词进度环、卡通吉祥物、整页 Spinner、展页变成黑色 Immersive Reader、强制 3D 仿真翻书、把展页做成左右双页杂志（桌面沉浸用**单页**）。

---

## 11. 实现验收清单（给 Cursor）

**工作态**

- [ ] 左目录按月 + 选中绿条 + 词数  
- [ ] 右页当日词条 / 空状态文案  
- [ ] 即时搜索，结果标「写于某日」，可跳转  
- [ ] 添加 / 设置 overlay + Key 本机说明  
- [ ] 词卡：章、引用、来源、改/AI/删 + 删除确认  
- [ ] 术语去重合并来源 + 对应 Toast  
- [ ] AI 解析仅条目内进行中  
- [ ] `/` `Ctrl+K` `Ctrl+N`  
- [ ] Toast 成功/合并/连不上本地服务  

**展页阅读**

- [ ] 从选中日进入；目录收起；单页纸居中放大  
- [ ] 按完整词条分页；页码 `当前/总计`（日内）  
- [ ] 左右热区 + 键盘 +（可选）横滑翻页  
- [ ] 翻过当日末页 → 下一有词日首页；反向对称  
- [ ] Esc/返回/点 desk 退出；日期选中同步  
- [ ] 风格 token 与工作态一致（纸/栏线/宋体/绿/朱红）  
- [ ] `prefers-reduced-motion` 降级  

**Zotero**

- [ ] 按钮四态（含失败）安静学术  

**视觉禁区**

- [ ] 无紫渐变 / 无底 Tab / 无大阴影卡片堆  

---

## 12. 建议实现顺序

1. Token + 字体 + 主骨架（展开书）  
2. 目录 + 当日列表 + 空状态 + 词卡  
3. 搜索 / 添加 / 设置 / Toast / 删除确认  
4. **展页阅读 + 分页 + 翻页 + 跨日**  
5. Zotero 按钮样式态  
6. 窄屏与减动效  

---

*文档版本：完整落地稿 · 含展页阅读。示意 PNG 见同目录 `mocks/`。*
