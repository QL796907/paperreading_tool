# Grokbot 图标设计提示词

把下面从「你是图标设计师」开始的整段交给 Grokbot。要它出多套方案，并且每套都配示例图，方便挑选。

---

你是图标设计师，兼做品牌符号。请为桌面软件「术语本 / Paper Glossary」设计应用图标，并给出**多套可挑选的方案**，每套必须配**示例图**（不要只写文字）。

## 产品

这是学生用的**本地论文术语笔记本**，配合 Zotero 读 PDF：选中专业名词 → 加入笔记本 → 按填写日期落页 → 可搜索、可手写解析、可用 AI 生成解析。

它不是背单词 App、不是 Notion、不是荧光高饱和效率工具。气质是：暗木桌上摊开的研究笔记；奶油纸、朱红栏线、墨色词头、森林绿只作一点点缀。

英文名 `Paper Glossary` 只作眉题；主名是中文「术语本」。图标主要给：

- Windows / 浏览器 favicon
- 桌面快捷方式
- 以后可能的应用商店小图标

需要在 16px、32px、256px 都可辨认。

## 必须遵守的视觉约束

**用得上的关键词：** commonplace book、稿纸、朱红栏线、词头/印章、奶油纸 `#F4ECD8`、墨 `#1F1A14`、朱红 `#C45C48`、森林绿 `#2F4F3E`、暗桌面 `#2C241C`。

**不要：** 紫渐变、霓虹、3D 玻璃拟态、卡通吉祥物、进度环、机器人头、一本翻开的书堆满装饰（钢笔、缎带、蜡封不要堆在一枚图标里）、左右双页杂志、黑色 Immersive Reader 风。

图标形态：圆角矩形或轻微圆角方印即可，圆角 ≤ 图标边长的 12%；也可以是方印/印章形。不要做成圆形社交 App 头像风。

## 请给出 6 套方案

每套方案必须包含：

1. **方案名**（2–6 个字）
2. **一句话概念**（为什么适合「术语本」）
3. **图形拆解**（由哪 1–2 个符号组成，避免超过两个主体）
4. **色板**（3–5 个 hex，尽量从上面的 token 里取）
5. **示例图（必须画出来）**
   - 一枚 **1024×1024** 主图标（透明或暗桌面底皆可，需注明）
   - 同一方案的 **32×32** 缩小预览（检验糊不糊）
   - 一枚放在暗木色 `#2C241C` 上的应用图标示意（像桌面快捷方式）
6. **不适合谁**：这套在什么情况下会弱（例如缩小时丢细节）

六套方向请刻意拉开，不要六张只差颜色：

A. **稿纸 + 朱红栏线**（最贴近现有 UI）  
B. **词典词头 / 方印「术」或「词」**（中文衬线，极克制）  
C. **合上的薄笔记本 + 一条栏线**（不要堆书脊装饰）  
D. **Zotero 选词瞬间**：一小段被点中的词 + 落入纸页（符号要简化到图标级）  
E. **森林绿点缀的学术印章**（像词条状态章「手写 / AI」的亲戚，但更像 App 图标）  
F. **你认为更好的第 6 套**：仍属纸质学术笔记，不要跳到科技蓝。

## 绘制要求

- 先出 6 套完整示例图，再写推荐。
- 每套主图标单独成图，不要六宫格糊成一张看不清的拼图；可以另附一张对比总览。
- 图形扁平、边缘干净，适合做成 favicon 和 `.ico`。
- 中文字（若用）必须是宋体/衬线结构，不要手写体、不要美术字描边彩虹。
- 不要在图标里写完整英文 Paper Glossary。
- 不要出现 Zotero 狐狸商标。

## 最后请你做的选择辅助

1. 用一张表对比 6 套：辨识度、学术感、缩小后清晰度、和现有 UI 的匹配。
2. 明确推荐 **1 个主选 + 1 个备选**，各用三句话说明为什么。
3. 主选方案再给：浅色底 / 暗色底 各一枚，避免桌面看不清。
4. 如果只能做一个 favicon，告诉我该保留哪一个图形细节、该删掉哪一个。

默认语言：简体中文。直接画图，不要先问我确认风格。
