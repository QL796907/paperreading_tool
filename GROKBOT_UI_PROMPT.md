# Grokbot UI 设计提示词

把下面整段复制给 Grokbot。它负责出视觉与交互稿；实现已经有一个可运行的本地术语本，设计需要对齐这些功能，而不是另起产品。

---

你是资深产品设计师，风格接近纸质学术笔记本，而不是 SaaS 仪表盘。请为「术语本 / Paper Glossary」做一套完整 UI 设计。

## 产品是什么

我是学生，用 Zotero 读论文。这是一个**本地网页笔记本**，专门收集论文里的专业名词。

核心场景：我在 Zotero PDF 里选中几个单词 → 选区弹出「加入术语本」→ 词条按**填写日期**记在笔记本的某一页。我也可以在软件里手写添加。每个词可以有解析：我自己写，或用我配置的 AI API 生成，生成后我还能改。必须能搜索，快速找到某个单词。

不要做成背单词 App，不要做成 Notion 克隆，不要做成荧光高饱和生产力工具。它应该像一本摊开的研究笔记：封面/目录在左，当日纸页在右，墨水、纸纹、日期、出处都要有。

## 必须覆盖的界面

1. **笔记本主界面（最重要）**
   - 左：日期目录（按月分组，显示当天词条数）
   - 右：某一天的词条页，像稿纸
   - 顶：搜索（搜术语、解析、论文名）
   - 添加术语、设置入口
2. **空状态**：今天还没有词。用一段短文告诉我：去 Zotero 选词，或在这里手写第一条。
3. **搜索结果态**：跨日期展示，每条旁边标原来写在哪一天。
4. **词条卡片**：术语（像词典词头）、解析正文、原文上下文引用、来源论文、状态章（待解析 / 手写 / AI 解析）、操作（改解析、AI 解析、删除）。
5. **添加术语 overlay**：术语必填；解析、上下文、来源论文可选；勾选「保存后立刻 AI 解析」。
6. **设置 overlay**：API 地址、Key、模型名、系统提示词、Zotero 新词是否自动解析。说明这是 OpenAI 兼容接口，Key 只存在本机。
7. **Zotero 选区弹窗按钮**：在 Zotero 自带高亮弹窗里加一颗不抢戏的按钮「加入术语本」。请单独画这颗按钮的样式：安静、学术、一眼能懂。成功/失败/进行中三态也要。
8. **Toast / 失败提示**：加入成功、重复词已合并来源、连不上本地服务。

## 交互约束

- 桌面优先，约 1280×800，也可给一版窄屏。
- 中文界面。英文产品名 Paper Glossary 只作为眉题。
- 同一术语再次加入不要变成两条，应合并来源、留在首次填写的那一页。
- 搜索要即时，不必单独结果页。
- AI 解析时词条内有进行中状态，不要整页转圈。
- 删除要确认。
- 键盘：`/` 或 `Ctrl+K` 聚焦搜索，`Ctrl+N` 添加。

## 视觉方向

关键词：commonplace book、线装/胶装笔记、奶油纸、朱红栏线、森林绿点缀、衬线中文、暗木桌面托着一本摊开的书。

请给出：

- 色彩 token（桌面、纸张、墨水、栏线、强调色）
- 字体建议（中文衬线 + 西文显示字体）
- 间距与栏线网格（让文字像写在横格上）
- 主界面线框 + 高保真描述
- 添加/设置 overlay 的纸片质感
- 空状态插画或文字排版方案（不要卡通吉祥物）
- 组件清单：日期行、词头、徽章、引用块、主按钮/幽灵按钮、搜索槽

## 请这样交付

1. 一段设计理念（不超过 120 字）
2. 完整色板与字体
3. 逐屏说明：布局、层级、文案、状态
4. 关键组件标注（尺寸、圆角、是否圆角——可以偏少，像印刷品）
5. Zotero 弹窗按钮的三态
6. 不要的东西：大阴影卡片堆、紫色渐变、无衬线仪表盘、底部 Tab 栏

默认语言：简体中文。先出设计系统，再出页面。若画图，请按桌面网页比例，不要手机 App 框架。
