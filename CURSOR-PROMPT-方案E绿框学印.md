# Cursor 改版任务：术语本 / Paper Glossary → 方案 E「绿框学印」

请把本应用的 **应用图标（含 favicon / 桌面快捷方式）** 和 **整站 UI 主题** 统一改成方案 E「绿框学印」风格。  
不只换图标：界面要像「奶油纸上的学术印章」——克制、印刷感、研究笔记，而不是 SaaS 仪表盘，也不是背单词 App。

---

## 0. 产品（勿跑偏）

本地网页「术语本」：配合 Zotero 收集论文专业名词；按填写日落页；可搜索；解析可手写或本机 AI。  
英文名 `Paper Glossary` 只作眉题；主名中文「术语本」。

气质：暗木桌上的奶油稿纸 + 学术印章。  
**不要：** 紫渐变、霓虹、3D 玻璃、卡通吉祥物、进度环、Notion 风、荧光效率工具、Zotero 狐狸商标。

---

## 1. 方案 E 视觉锚点（图标）

### 1.1 图标图形（必须）
- 圆角矩形，圆角 ≤ 边长 **12%**（偏方印，不要社交圆形头像风）。
- 底：奶油纸 `#F4ECD8`（可极轻纸纹，勿喧宾夺主）。
- **主结构：森林绿 `#2F4F3E` 方框印章**（单线或双细线方框，居中）。
- 框内：墨色 `#1F1A14` 极简书页/词条块符号（一个主体即可）。
- 点缀：框内一角小朱红 `#C45C48` 方印点（像角章，面积很小）。
- **禁止：** 图标内写「Paper Glossary」「术语本」全文；不要复杂拉丁铭文；不要动物；不要钢笔/缎带/蜡封堆砌。

### 1.2 小尺寸策略
- 32 / 16px：保留「奶油底 + 绿方框 + 中心墨块 +（可选）朱红点」。
- 删掉纸纹、双线变单线、内部符号可收成实心竖矩形。

### 1.3 图标生成提示词（若需 AI 出图再矢量化）
```
App icon, rounded square corners ≤12%. Cream paper background #F4ECD8.
Centered forest-green #2F4F3E square academic seal frame (thin double border).
Inside: minimal ink #1F1A14 open-book or lemma block silhouette.
Tiny vermillion #C45C48 square chop accent in one inner corner.
Flat, scholarly, printable, no text, no animals, no fountain pen clutter, no purple neon.
```

### 1.4 导出
- favicon：16 / 32 / 48 PNG + `favicon.ico`
- 桌面 / PWA：192 / 512，及 1024 主视觉
- 深色桌面预览：图标落在 `#2C241C` 上仍清晰

---

## 2. 设计 Token（整站 UI 跟图标同源）

| Token | Hex | 用途 |
|--------|------|------|
| `--desk` | `#2C241C` | 外侧桌面衬底（若有） |
| `--paper` | `#F4ECD8` | 主纸面背景 |
| `--paper-aged` | `#EBE2CC` | 次级纸、目录底、斑马行 |
| `--ink` | `#1F1A14` | 主文字、词头 |
| `--ink-muted` | `#6B5E4E` | 次级说明、日期、出处 |
| `--ink-faint` | `#A89880` | 占位、禁用 |
| `--seal` | `#2F4F3E` | **主强调：森林绿**（印章框、选中、主按钮、焦点） |
| `--seal-soft` | `#E4EDE7` | 绿浅底、选中行、Chip |
| `--rule` | `#C45C48` | 朱红栏线、引用竖线、危险/强调线（点缀，勿大面积铺） |
| `--rule-soft` | `rgba(196,92,72,0.35)` | 稿纸横格 |
| `--ai` | `#2F4F3E` | AI 解析章（绿印） |
| `--hand` | `#5C4033` | 手写章 |
| `--pending` | `#8B6914` | 待解析章 |
| `--danger` | `#8B3A2F` | 删除确认 |
| `--stroke` | `rgba(47,79,62,0.18)` | 分割/描边（偏绿灰，不要纯 UI 灰） |
| `--shadow` | `rgba(44,36,28,0.14)` | 极轻纸影 |

**相对旧方向的变化：**  
- 主强调从「朱红主导」改为 **森林绿印章感主导**；朱红退回栏线/引用/小角章。  
- 仍是奶油纸学术笔记，不是白底绿按钮的后台系统。

### 2.1 字体
- 中文：思源宋体 / Noto Serif SC（词头可 SemiBold）
- 西文眉题 Paper Glossary：EB Garamond / Cormorant Garamond
- 等宽（API）：IBM Plex Mono
- 正文行高约 1.75；词头 1.25

### 2.2 形状
- 圆角尽量少：纸页/按钮/章 ≤2–4px（偏印刷品，不要大胶囊）
- 状态「章」：方印描边，不要 iOS pill
- 主按钮：森林绿底 + 奶油字；幽灵按钮：绿描边或墨色文字链

---

## 3. UI 改版范围（必须跟风格）

对齐现有功能，不要另起产品。至少覆盖：

1. **笔记本主界面**（左日期目录 + 右当日词条）
2. **空状态**
3. **搜索结果态**
4. **词条卡片**（词头、解析、引用、来源、状态章、操作）
5. **添加术语 / 设置 overlay**
6. **删除确认、Toast**
7. **展页阅读模式**（若已实现：单页纸翻页；绿作页码/返回强调，纸面仍奶油）
8. **Zotero「加入术语本」按钮态**（安静、学术；成功可用森林绿字色）

### 3.1 主界面要点
- 外：暗木 `--desk`；内：摊开奶油书本
- **选中日期：** 左侧改为 **森林绿竖条**（4px）+ `--paper-aged` 底（与图标绿印呼应）
- **朱红：** 保留为稿纸栏线、词头短下划、引用左竖线——点缀，不抢绿
- **主按钮 / 关键操作 hover：** `--seal`
- 搜索聚焦：底边或光标环用 `--seal`，不要紫色

### 3.2 状态章（印章家族，服务方案 E）
| 状态 | 样式 |
|------|------|
| 待解析 | 描边章，色 `--pending` |
| 手写 | 描边章，色 `--hand` |
| AI 解析 | 描边章，色 `--seal`（绿） |
| 解析中 | 绿字 + 静态点，整页不转圈 |

章：方印、1px 描边、高约 20–22、圆角 ≤2px。

### 3.3 Overlay
- 幕布暗木半透明；纸片奶油直角/2px
- 主 CTA「保存」：森林绿实心
- 设置说明句保持：「OpenAI 兼容；密钥只在本机」

### 3.4 展页阅读
- 单页奶油纸居中；顶栏「返回」可用绿文字链
- 页码 `--ink-muted`；翻页热区不要霓虹
- 动效克制；尊重 `prefers-reduced-motion`

---

## 4. 实施顺序

1. 写入/替换全局 CSS 变量或主题文件（上表 Token）。  
2. 主色从朱红主导改为 **绿印主导 + 朱红点缀**；检查所有 Primary / focus / selected。  
3. 词条章、目录选中条、主按钮、链接 hover 统一到 `--seal`。  
4. 替换 favicon / PWA / 桌面图标为方案 E。  
5. 截图验收：主界面、空状态、设置、展页、图标 32px 与暗桌面。

---

## 5. 验收清单

- [ ] 图标：奶油底 + 绿方印框 + 墨色简符 + 小朱红角点；无英文全称、无狐狸标  
- [ ] 16–32px 仍能认出「绿框」  
- [ ] UI 默认奶油纸；选中/主按钮/焦点为森林绿  
- [ ] 朱红仅作栏线/引用/小章点缀，不大面积铺底  
- [ ] 状态章为方印族，AI 章为绿色  
- [ ] 无紫渐变、无大阴影卡片堆、无底部 Tab  
- [ ] 深色桌面上图标与纸面对比足够  

---

## 6. 不要做

- 不要只换图标不换 UI  
- 不要做成白底 Material 绿的后台 Admin  
- 不要把朱红删光（栏线还在，只是不当主色）  
- 不要双页杂志展页；展页保持单页纸  
- 不要引入短视频/效率荧光风  

---

## 7. 完成后请回报

1. 主题/Token 文件路径  
2. 图标与 favicon 路径  
3. 截图：图标（方）、暗桌面、主界面、一词条页（含绿章）各一张  
