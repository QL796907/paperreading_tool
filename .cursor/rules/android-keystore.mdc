---
description: 换电脑打 APK 时必须带上签名钥匙，并提醒 hy
alwaysApply: true
---

# Android 签名钥匙

hy 要求：换电脑时必须提醒这一点。

手机 APK 用 `android/glossary.keystore` 签名（密码在 `android/keystore.properties`）。这两份都不进 git。

- 换电脑、重装系统、新克隆仓库后打 APK 前：先把这两份文件拷到 `android/`，再用原来的钥匙打包。
- 不要在新电脑上重新生成一把钥匙。新钥匙签的包装不到已经装过术语本的手机上（不能当更新）。
- 打包脚本若发现没有 keystore，应停下来提醒，而不是默默新建。
