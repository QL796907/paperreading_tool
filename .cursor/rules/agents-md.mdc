---
description: 没有 AGENTS.md 就创建；每轮更改目标做完后只更新真正有变化的部分
alwaysApply: true
---

# AGENTS.md

- 仓库根目录必须有 `AGENTS.md`。没有就立刻创建，给后续 agent 看：项目是什么、目录怎么走、怎么跑、怎么发版、有哪些硬规则。
- **每一轮更改目标做完后** 更新 `AGENTS.md`：只改本轮真正变了的段落（新目录、新命令、新平台、流程变了）。
- 不要整篇重写。没变的结构、命令、密钥说明保持原样。
- 用中文，写给下一个进来的 agent，不要写成用户 README 的副本。
