---
description: 第一次发版写精美 README；之后每次发版更新 README
alwaysApply: true
---

# 发版时的 README

只有 hy 说 **发版** 时才动用户 README。日常改功能不要为了交差去改 `README.md`。

## 第一次发版

把根目录 `README.md` **整篇写成给会下载、但不懂代码的人看的说明**（中文、一两分钟能读完，不是 `AGENTS.md` 的副本）：

- 先说这是什么、能帮什么忙
- 再说下哪个包、怎么安装打开、第一次怎么用
- 最后补几条真会卡住人的小提示（托盘没退出、手机不能划词、blockmap 不用下）
- 语言轻松、通顺、无歧义；可保留小图标，不要夸张
- **不要写**技术栈、编译命令、npm、API 地址表、贡献指南、目录细账、密钥

## 之后每次发版

在现有 README 上 **增量更新**，不要整篇重写：

- 版本号、安装包文件名、Release 链接改成这一版
- 新功能、变了的安装/更新步骤写进去
- 过时段落删掉或改对

没改 README 的发版不算发完。
